{
  "name": "escala-voluntarios-2026",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "lint": "echo 'No linting needed for single-file HTML project'"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
